import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";
import SmoothScrollProvider from "@/components/providers/SmoothScrollProvider";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
});

export const metadata: Metadata = {
  title: {
    default: "WanderBase — Discover the World",
    template: "%s | WanderBase",
  },
  description:
    "WanderBase is your ultimate travel companion. Explore 120+ handcrafted destinations, book curated tour packages, and create memories that last a lifetime.",
  keywords: ["travel", "tours", "destinations", "vacation", "adventure", "luxury travel"],
  openGraph: {
    title: "WanderBase — Discover the World",
    description: "Explore 120+ handcrafted destinations with WanderBase.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body className="font-sans antialiased">
        <SmoothScrollProvider>
          <Navbar />
          <main>{children}</main>
          <Footer />
        </SmoothScrollProvider>
      </body>
    </html>
  );
}
